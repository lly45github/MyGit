package com.utils

import java.util.Properties

import scala.collection.mutable

class PropertiesCenter {
  /*var propMap = new mutable.HashMap[String,Properties]();
  def getProp(key: String): Unit ={
    Properties prop = propMap(key);
    if(prop==None){

    }
  }*/

}

object PropertiesCenter{

}
